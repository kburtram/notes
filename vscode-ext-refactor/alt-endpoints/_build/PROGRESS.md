# TypeScript Query Endpoint — Progress Journal

Journal wins over EXECUTION_PLAN.md on conflict. Newest entries at the bottom.
Format: `## <n> — <date> — <task> — <status>` then facts: what changed, evidence, decisions, next.

## 1 — 2026-07-13 — Setup — DONE

- Working tree `C:\repos\test\langsrv`; heads: vscode-mssql `37396c3c44`, sqltoolsservice `88c6149bce`, perftest `83e017b95e` (all dev/query, clean).
- Read `typescript_query_endpoint_addendum.md` (normative, TSQ2-n) and `querystudio_web_backend_addendum.md` §3 + WEB2-1/2 (shared foundation to implement here, once).
- Confirmed shared registry has not landed: `services/sqlDataPlane/` = api.ts, fakeBackend.ts, sqlDataPlaneService.ts, vscodeSqlTokenSource.ts only.
- Node v24.17.0 / npm 11.13. `npm install` for vscode-mssql monorepo started in background.
- Session task list #1–#17 created mapping TSQ2-0..15 + FOUND-1/2. Decision log seeded D1–D4 in EXECUTION_PLAN.md.
- Next: TSQ2-0 packaging spike once install completes (pin tedious exact, read bundle-extension.js, lazy-chunk decision, measure import).

## 2 — 2026-07-13 — TSQ2-0 packaging spike — DONE

- SqlClient baseline verified: **6.1.5** (`sqltoolsservice/Packages.props:86`) — addendum §2.1 confirmed; D4 stands.
- tedious pinned **exactly 20.0.0** in `extensions/mssql/package.json` (npm --save-exact; lockfile updated; Node v24.17.0 vs engines >=22 ok).
- Bundler: esbuild CJS multi-entry (`scripts/bundle-extension.js`); `vectorAnalysisWorker` is the existing separate-lazy-bundle precedent.
- Measurements (cold, this machine): unbundled `require('tedious')` **1627 ms / +15.1 MiB heap** (validates §2.9 hard); esbuild minified single-file bundle **176 ms / +8.2 MiB, 1.5 MB**; plain bundle 201 ms / 2.9 MB. tedious bundles cleanly (no resolution errors, no native modules).
- Decision **D6**: dedicated minified chunk `dist/tsNativeProvider.js`, computed-path require on first selection; zero bytes/parse added to activation. Final VSIX-size + packaged-smoke evidence lands with TSQ2-14.
- api.ts contract extended (FOUND-2 groundwork, additive): SqlCapabilityId/Value/Set/Requirement + MissingCapability + CapabilityCheck.missingDetail/alternatives; QueryAcceptance + QueryHandle.accepted (backendQueryId deprecated); QueryCompleteSummary.outcomeCertainty/outcomeReason; error codes CapabilityUnsupported/PolicyDenied/ResourceLimit/Client.Aborted/Transport.Closed/Transport.Backpressure/Provider.Internal; OpenSessionParams.requiredCapabilities (requestedCapabilities deprecated).
- Next: capabilityRegistry.ts + backendFactory.ts + SqlDataPlaneService v2 (FOUND-1/2), then implement `accepted` in Sts2Backend/FakeBackend and run existing suites.

## 3 — 2026-07-13 — FOUND-1/2 shared foundation — CODE COMPLETE, tests running

Files (all `extensions/mssql`):
- `src/services/sqlDataPlane/api.ts` — additive: SqlCapabilityId (38 ids incl exec.oneActiveQuery/messages.*/plan.* beyond web minimum), SqlCapabilityValue/Set/Requirement, MissingCapability, CapabilityCheck.missingDetail+alternatives, QueryAcceptance + QueryHandle.accepted (backendQueryId deprecated), outcomeCertainty/outcomeReason, requiredCapabilities, 7 new error codes.
- `src/services/sqlDataPlane/capabilityRegistry.ts` — NEW: STRUCT_FIELD_TO_CAPABILITY (exhaustive), booleanProjection/setFromNegotiated bridges, evaluateRequirements (support/fidelity/limit; unknown never satisfies), mergeCapabilitySets, answerFromSet (+opt-in flag vector/spatial).
- `src/services/sqlDataPlane/backendFactory.ts` — NEW: SqlBackendIdentity tuple, SqlBackendFactory (realmClass, staticCapabilities, fingerprintSettings), normalizeBackendKind (sts2-jsonrpc alias; unknown→undefined), BackendEntrySnapshot.
- `src/services/sqlDataPlane/sqlDataPlaneService.ts` — REWRITE: multi-entry registry (single-flight, retry-after-failure, per-kind fingerprint, stale-swap-at-zero-sessions via handleConfigurationChanged, bounded dispose w/ unref), registry view wraps openSession for explicit session registration/finalization (close/dispose hooks + state events), canOpen/openSession evaluate requirements (profile-derived auth + explicit) BEFORE factory.create/credentials, capability oracle (provider/session/any + alternatives), passive statusSummary (D5), registerFactory seam for ts-native self-registration, honest sts2-local static statement (initialize-negotiated facts = conditional), fake static statement + auth ids fix.
- `src/services/sts2/sts2Backend.ts` — accepted settles on execute response/rejection; synthesized terminals settle aborted + outcomeCertainty unknown/transportLost|cancelUncertain.
- `src/services/sqlDataPlane/fakeBackend.ts` — FAKE_CAPABILITIES exported; accepted settles at run start; terminal-before-run settles aborted.
- `src/diagnostics/diagnosticsCore.ts` — 7 new codes in SAFE_ERROR_CODES.
- `package.json` — backend enum: sts2-local (default) | ts-native | fake | sts2-jsonrpc(deprecated alias), future-sessions wording.
- `test/unit/sqlDataPlaneRegistry.test.ts` — NEW: mapping exhaustiveness/round-trip, requirement evaluation (fidelity floors, limits, unknown), merge precedence, oracle answers, kind normalization, duplicate registration, single-flight+retry, passive status zero-construction, multi-local coexistence + session accounting, stale-config swap, credential tripwire (create + providers uncalled), explicit requirements + alternatives, fake acceptance.
- `test/unit/vectorSearchService.test.ts` — mock handle gained accepted (only compile fallout in repo).

Notes: perftest built in langsrv (clears pre-existing perftest-inproc typecheck errors). tsgo typecheck fully green. Interpretation recorded: rejected executes still deliver the failed terminal through sink.onComplete (existing behavior, completion liveness) — web §3.4 "no stream events" read as result/rows/messages.
- Next: vscode-test run (registry+conformance+sts2Backend files) → fix → commit `dp:` FOUND-1+2.

## 4 — 2026-07-13 — FOUND-1/2 COMMITTED + TSQ2-2 COMMITTED

- FOUND-1/2: 67 unit tests green (new sqlDataPlaneRegistry suite + existing conformance + sts2Backend wire suites). Commit `730850700` "dp: land shared provider registry, capability contracts, and pinned tedious dep" (11 files, +2035). Precommit hooks (prettier/CRLF/l10n) reformatted files — always Read before Edit from here on.
- TSQ2-2: `src/services/tsNative/driver/{tdsDriver,fakeTdsDriver}.ts` + `test/unit/tsNative/fakeTdsDriver.test.ts` (8 tests green). Commit `168048af3` "tsq2: add ITdsDriver port and deterministic fake TDS driver".
  - Port: TdsQueryEvent union w/ driverSeq, generation leases (accepted/completed always settle), TdsErrorCategory, pause reasons, TsNativeFaultProfile.
  - Fake: scripted opens/queries, bounded pause overrun (default 1 row), per-row virtual tick pacing, sever/hang/fault knobs, listener accounting, VirtualClock (advance = drain-microtasks-before-every-timer-scan — ordering bug found+fixed by test).
- Tasks #1-#4 complete. In progress next: TSQ2-4 engine (queryEngine/resultSetLedger/pageBuilder/boundedEventLane + session/backend skeleton so the conformance third binding can register), then TSQ2-5 backpressure, then TSQ2-3 tedious adapter + live smoke.
- Key commands: typecheck `npm run build:extension:typecheck`; emit `npm run build:extension:emit`; run one suite `npx vscode-test --run out/test/unit/<file>.test.js` (all in extensions/mssql).

## 5 — 2026-07-13 — TSQ2-4/5 engine COMMITTED (`9c8470288`)

- `src/services/tsNative/`: queryEngine.ts (lifecycle, absolute domain deadline, atomic cancel-cause terminals, cancel-ack + hard deadlines w/ forced abort, fidelity fail-closed pre-first-page, CPU slice governor, window+message backpressure, ResourceLimit backstop, terminal aggregates), resultSetLedger.ts (DONE=statement boundary; rowsAffected=DML DONEs closing no set), pageBuilder.ts (STS2 shaping parity; oversized row=own page; addRow returns 0..2 pages), boundedEventLane.ts (one-in-flight, sink deadline containment, count+byte budgets), cellEncoder.ts (typeHints lockstep, sha256 truncation markers, UTF-8-safe prefixes, fidelityViolation: decimal p>15/money/datetimeoffset; smallmoney+p≤15 decimal pass w/ fixed-scale render), tsNativeSession.ts (router, Busy, bounded close+destroy), tsNativeBackend.ts (open deadline over secret resolution, abandoned-open destroy, typed open errors, integrated refusal, TS_NATIVE_CAPABILITIES honest v1: vector/spatial/capture false).
- 92 tests green total; 17-case engine conformance incl timeout-after-first-response (§2.4), dispose-drops-sink-delivery (documented: onComplete not delivered after dispose; completion promise carries summary), sessionClose→synthesized connectionLost (§5.4 — classifier fix), message flood bounded via pause (ResourceLimit backstop unreachable when driver pausable — by design).
- New error identity `SqlDataPlane.QueryFailed` (server-originated) in api.ts + safe-code set.
- REMAINING from TSQ2-5 scope: memoryBudget.ts / §5.13 memory-pressure circuit breaker — deliberately deferred to TSQ2-8 (needs the runtime sampling infra); task #7 stays open until then.
- Next: TSQ2-3 — tediousDriver.ts adapter (§5.5 baseline options: requestTimeout=0, rowCollection*=false, useColumnNames=false, execSqlBatch, ECANCEL/ETIMEOUT mapping, done-token rowCount validity, colmetadata normalization incl udtName, databaseChange/error/end routing, listener cleanup), providerEntry.ts lazy bundle (bundle-extension.js entryPoint `tsNativeProvider`), ts-native factory registration in sqlDataPlaneService (computed-path require + Node compat check), live smoke vs STS2_SQLSERVER_CONNSTRING (skip-not-fail).

## 6 — 2026-07-14 — TSQ2-3 COMMITTED (`efcf18dd8`)

- tediousDriver.ts (only tedious import in repo; structural TediousColumnMeta to stay version-tolerant), providerEntry.ts + esbuild entry `tsNativeProvider` (dist chunk builds: 2.91 MB dev / ~1.5 MB prod-minified), ts-native factory in sqlDataPlaneService (conservative static caps w/ reason codes; Node>=22 check; computed-path loader: dist/tsNativeProvider.js → out fallback), tediousLive.test.ts (9 live cases, skip-not-fail).
- **ENV GAP (Karl-actionable):** `STS2_SQLSERVER_CONNSTRING` is NOT set on this machine (checked shell/user/machine scope) — live lane is pending. Port 1433 HAS a listener, so likely just the missing envvar. When set, run: `npx vscode-test --run out/test/unit/tsNative/tediousLive.test.js` in extensions/mssql. Golden-cell parity (TSQ2-6 gate) and Entra lane (TSQ2-7) also depend on live lanes (`STS2_AZURESQLSERVER*` too).
- Commits so far on dev/query (langsrv/vscode-mssql): `730850700` dp foundation, `168048af3` driver port, `9c8470288` engine, `efcf18dd8` tedious adapter. 100 unit tests green in repo (92 + 8 driver-port).
- Task state: #1-#6 done; #7 open (only §5.13 memory-pressure breaker left → TSQ2-8); next up: TSQ2-8 (observability/status/Debug Console/capsule + memoryBudget), TSQ2-9 (capability UX + per-document selection + fallback prompt), TSQ2-12 (perftest Path A backend pairs — needs live SQL to actually RUN, plumbing can land), TSQ2-6 golden parity (needs live), TSQ2-7 Entra (needs live lane), TSQ2-10/11 transcoders, TSQ2-13 consumers, TSQ2-14 gate.
- Manual smoke recipe (when env ready): settings `mssql.sqlDataPlane.enabled=true`, `mssql.sqlDataPlane.backend="ts-native"`, `mssql.queryStudio.enabled=true` → open Query Studio, connect with a SQL-login profile, run a query; status via `MS SQL: Show SQL Data-Plane Status` (passive; shows per-entry registry state incl ts-native).

## 7 — 2026-07-14 — FULL-SUITE VALIDATION + TSQ2-9 slice 1 COMMITTED (`ac002e7d0`)

- **Full unit suite: 4936 passing / 3 failing — all 3 PROVEN pre-existing** on head `37396c3c4` (git diff across tsq2/dp commits shows NO changes to their code/tests): sqlScripting strict host (drop capability list), sqlLanguage static system catalog (empty sys completion source), OE v2 stableProfileId ('srv|Db|u||SqlLogin' extra segment). CopilotChatEntry + bundle-budget + localization failures from the first run were fresh-checkout artifacts (missing full build) — cleared by `npm run build`; CopilotChatEntry passes in isolation (host-contention flake under full suite).
- **Tell Karl:** the 3 pre-existing failures look like real (small) bugs on dev/query worth a look in the main checkout.
- TSQ2-9 slice 1: providerSuggestions.ts (prompt/auto/off matrix, vscode-free, 6 tests), documentSessionBinding routed through registry canOpen→fallback→openSession(backendKind), per-document override API (documentBackendOverride/setDocumentBackendOverride/activeBackendKind), `mssql.sqlDataPlane.capabilityFallback` setting, displayNameFor. REMAINING TSQ2-9: QsState capability derivation + capabilityHints, providerTypeName in ColumnMetadata→QsResultColumn, plan-toggle gating, picker command `mssql.sqlDataPlane.pickDocumentBackend` (needs queryStudioController wiring → do with TSQ2-13), export/AI exactness gates.
- Next increments (in order): TSQ2-8 observability.ts (EngineObserver→diag spans, provider-neutral events §9.1 + tsNative family §9.2, status via backend.snapshot() into statusSummary, memoryBudget §5.13) → Debug Console page + capsule; TSQ2-12 perftest registerBackendPair (plumbing landable without live SQL); transcoders TSQ2-10/11; live-gated: TSQ2-6 golden parity, TSQ2-7 Entra.

## 8 — 2026-07-14 — TSQ2-8 slice 1 COMMITTED (`aae98bbd5`)

- observability.ts: createDiagEngineObserver (one aggregate terminal event/query `sqlDataPlane.tsNative.query.terminal` w/ rows/pages/bytes/encodeMs/sinkWaitMs/pauseMsByReason/yields/maxSlice/first* timings/outcomeCertainty/errorCode; invariantViolation events; post-terminal drops counted not emitted) — wired as providerEntry default observer; engine stays diag-free. Emit shape uses entity {kind:"query", id}.
- Commit list (7): 730850700 dp-foundation, 168048af3 port+fake, 9c8470288 engine, efcf18dd8 tedious adapter, ac002e7d0 fallback+override, aae98bbd5 observability slice.
- NEXT STRETCH START HERE: (a) TSQ2-8 rest: statusSummary per-entry detail via backend.snapshot(), Dc* RPC + Debug Console "SQL Data Plane" page (console-hosted host pattern: getState/dispatchAction/onDidChange throttled — model completionsDebugConsoleHost.ts), support capsule (supportCapture.ts), memoryBudget §5.13 sampling + ResourceLimit breaker, overrides.ts (`mssql.sqlDataPlane.tsNative.overrides`: tuning + capabilityMask force-OFF-only + faults via decorator over ITdsDriver), observability-contracts registration (perftest/packages/observability-contracts + regenerate vendored observabilityContract.generated.ts — check conformance test coverage of diag types first); (b) TSQ2-12: perftest registerBackendPair + paired querystudio scenarios (registry.ts ~line 940 querystudio-query-10k as template; userSettings differ only in mssql.sqlDataPlane.backend); (c) picker command + QsState capability derivation w/ TSQ2-13.

## 9 — 2026-07-14 — AUTONOMOUS RUN (Karl: no checkpoints until done). LIVE LANES UP. TSQ2-5 CLOSED, TSQ2-10/11 DONE

- Karl mid-turn: env vars EXIST — real names: **STS2_SQLSERVER_SQLLOGIN_CONNSTRING** (localhost SQL25; full perftest harness available), STS2_AZURESQLSERVER_CONNSTRING (azure sqlauth), STS2_AZURESQLSERVER_ENTRAID_CONNSTRING (machine scope), MSSQL_PERFTEST_CENTRAL*. Memory file name was stale. Use on-prem + azure sqlauth lanes per Karl; Entra one "works" too.
- Commit `1a28c7753`: memoryBudget breaker (§5.13, engine trips ResourceLimit at page points), overrides.ts (parse/mask-allowlist/faults + withFaults decorator + measurement-taint detection), factory wiring (setting fingerprinted), providerEntry applies all, **tediousDriver canonicalTypeName from EMPIRICAL probe** (IntN len→tinyint..bigint, BitN, DecimalN, MoneyN 4/8, FloatN 4/8, DateTimN, 65535=max sentinel), live suite renamed env var → **ALL 9 LIVE TESTS GREEN vs local SQL 2025**.
- Commit `a6e481012`: TSQ2-10/11. PROBE RULINGS: vector→identity-less VarChar sci-notation text (§6.8: NEVER advertised, transcoder identity-keyed for future; capability reason driver.noWireTypeIdentity); spatial→UDT w/ udtInfo.typeName geometry/geography + CLR payload. spatialTranscoder.ts (MS-SSCLRT v1, Z/M, rings, multi/collection, geography lat-long swap, curves→unrenderable, bounds-checked) + vectorTranscoder.ts + engine/policy/hints/ColumnMetadata wiring (providerType=udtName added for capability UX). **spatialWkbV1=true** (live round-trip: polygon-with-hole + geography linestring WKB BYTE-IDENTICAL to server STAsBinary()). 10 transcoder tests + live spatial test green.
- Task state: #1-7,#13 done. Remaining: #8 (TSQ2-6 golden matrix — extend live suite), #9 (TSQ2-7 azure lane + canaries; Entra-interactive deferred), #10 (TSQ2-8: status detail/Dc host/capsule/contracts), #11 (TSQ2-9 rest: picker cmd + QsState derivation), #14 (TSQ2-12 perftest pairs + REAL A/B RUN on localhost), #15 (TSQ2-13 consumers), #16 (TSQ2-14 gate: no-tedious-in-activation guard test via metafile, soak, VSIX, full suite, checklist).

## 10 — 2026-07-14 — FEATURE-COMPLETE STRETCH (autonomous run, final entry)

Commits this stretch (langsrv/vscode-mssql dev/query): `1a28c7753` (breaker+overrides+type-normalization+live-green), `a6e481012` (transcoders, spatialWkbV1 live-proven), `a9758a0e2` (golden matrix + canary + Azure lane), picker/threading/status commit, `3bd7c748c` (guard+soak+leak fixes). perftest: `eb92494` (A/B pair + FIRST RESULTS).

**FIRST A/B RESULTS** (run 2026-07-14T03-39-58Z_ae3ed9cc, 8/8 passed, 3 official reps each, local SQL 2025, span-split):
- submit→complete (engine, monotonic): **ts-native ~177ms vs sts2 ~232ms (−24%)** — the zero-serialization win, real.
- submit→resultsRendered: ts-native ~274ms vs sts2 ~326ms (−16%).
- scenario.wallclock: +8.4% for ts-native (pre-submit harness dispatch noise dominates; n=3 exploratory).
- Reports: perf-runs/2026-07-14T03-39-58Z_ae3ed9cc/index.html + head-to-head-querystudio-query-10k-sts2-vs-querystudio-query-10k-tsnative.html.
- The ts-native treatment ran the FULL stack in real VS Code (QS connect, metadata hydration on ts-native, execute, 10k webview render, markers, noErrors) — consumer-integration proof.

**Soak found a real leak class** (Promise.race deadline timers never disposed when the other branch won — open/cancel-ack/dispose-drain/session-close, ~1.7 timers/cycle): all four sites fixed; 300-cycle plateau green (0 sessions, 0 open connections, <10 pending timers). Packaging guard test: extension.js has 0 `node_modules/tedious` markers, provider chunk 284.

**§17.1 ENGINEERING-PREVIEW CHECKLIST:**
- [x] lazy package from packaged VSIX (chunk built; VSIX verification in final gate run)
- [x] activation not regressed when unselected (guard test: zero tedious in activation bundle; lazy require on selection only)
- [x] shared conformance passes (registry+fallback+engine suites; FakeBackend+Sts2+TsNative)
- [x] SQL Login + one cloud lane pass (local SQL2025 12 tests + Azure sqlauth 2 tests; Entra-interactive lane deferred — token PATH unit-tested, needs signed-in VS Code session)
- [x] supported scalar/type fixtures exact (golden matrix: bigint>2^53, dt2(7) sub-ms, time(7), guid casing, unicode, hex, truncation digests)
- [x] unsupported exact types fail BEFORE rows w/ provider alternative (decimal(38) live test + fallback flow)
- [x] timeout-after-first-response/cancel/dispose/close/loss settle exactly once (engine conformance + live)
- [x] queues/tasks/listeners bounded; soak plateau (300 cycles)
- [x] privacy canaries pass (canary password through failed login: error/JSON/snapshot clean)
- [~] support capsule — PARTIAL: status snapshots + terminal aggregates + overrides visibility shipped; Debug Console webview page + capsule EXPORT remain (see residuals)
- [x] tri-provider perf report exists (two treatments now; sts2-remote slot reserved) + interaction non-inferiority pending broader matrix

**RESIDUALS (scoped follow-ups, in priority order):**
1. TSQ2-8 remainder: Debug Console "SQL Data Plane" webview page (console-hosted host getState/dispatchAction per completionsDebugConsoleHost pattern; state = statusSummary().details + capability matrix + overrides), support-capsule export command (ring buffer via FeatureCaptureStore + traceCodec), observability-contracts registry entries for sqlDataPlane.tsNative.* diag family (conformance test currently only greps Perf.* markers — passed green — but registering is the honest §9.2 posture).
2. TSQ2-9 webview niceties: QsState capabilityHints + suggest-switch UX on Spatial/Vector tabs (formatCapabilitySuggestion helper is ready; providerType now rides ColumnMetadata).
3. Perftest Path B: treatmentFingerprint in PerfResult, run-level userSettings matrix, firstPage/rows-per-sec product markers (engine already computes firstPageProduced/AcceptedMs — emit as Perf markers under PERF_MODE), identity phase-map for same-family head-to-head, broader A/B matrix (100k/wide/large-cells/messages/interaction + soak) per §12.8.
4. TSQ2-15/GA: PLP streaming or read-limit policy, decimal/dto driver patches (TSQ2-U*: tedious #678 decimal-as-string, dto offset), packaged multi-platform matrix, dogfood + §17.3 default-flip evidence.
5. Entra-interactive live validation (needs signed-in VS Code auth session; wire = vscodeSqlTokenSource → tokenProvider → azure-active-directory-access-token, all unit-tested).

Task statuses finalized: #10 stays OPEN (residual 1); #11/#12/#14/#15 completed w/ notes above; #16 completes on final-gate green (full suite + VSIX, running in background at entry time).

**FINAL GATES (2026-07-14): CLOSED.** VSIX packaged (mssql-1.44.0.vsix, 83.8 MB; extension/dist/tsNativeProvider.js ships as its own 2.91 MB entry beside extension/dist/extension.js). Full unit suite: **4969 passing / 16 pending / 4 failing — all 4 known non-build failures** (sqlScripting strict-host, sqlLanguage sys catalog, OE v2 stableProfileId — all proven pre-existing by diff; CopilotChatEntry — passes in isolation, full-suite host-contention flake). Zero failures in tsNative/sqlDataPlane/sts2 suites incl. 14 live tests. Engineering-preview gate (§17.1) COMPLETE except the two scoped partials noted in the checklist (Debug Console page + capsule export → task #10 / residual 1).

## 11 — 2026-07-14 — DOGFOOD FIXES + Debug Console page (autonomous)

Karl dogfooded the preview and reported 4 items + a persistent tab bug. Findings and fixes (langsrv/vscode-mssql dev/query):

**Diagnostics were BLIND (root cause of "no details" + why #3/#4 were hard to see).** The lazy provider chunk bundles its OWN copy of diagnosticsCore, so the `diag` reachable from providerEntry had ZERO sinks — every `sqlDataPlane.tsNative.*` event emitted into a dead singleton. Karl's journal had 0 ts-native events despite live runs. **Fix `c71d116ae`:** build the diag observer on the ACTIVATION side (sqlDataPlaneService.tsNativeFactory injects `createDiagEngineObserver()`) and pass it into `provider.createBackend`; observability.ts engine/api imports made `import type` so activation pulls no tedious (guard still 0/284). Regression test pins observer→diag emission + status mapping + privacy.

**Tab stranding on Messages (Karl: `SELECT 100` goes to Messages, "keep asking to fix").** `resolveQueryStudioVisibleTab` redirected requested=Results→Messages on ANY transient empty-metadata render when not executing, and the eligibility effect writes that back into activeTab → STICKY. The 790c63cc1 guard only covered the executing phase; fast queries are first OBSERVED already terminal (debounced pushes coalesce), and back-to-back runs briefly show a completed kind with cleared summaries. **Fix `5d89c2ed5`:** Results is the always-present home tab — never resolver-redirected to Messages; the terminal-state handler (app.tsx) remains the single authority that moves a COMPLETED no-data/errored run to Messages. Contributed tabs keep fall-back. Tests updated.

**#3/#4 (parse button / error messages don't display) — engine PROVEN correct, not a data-loss bug.** Live probes against compiled out/src (scratchpad/probe-error-shapes.js, probe-raw-parseonly.js): plain syntax error → `onMessage` kind=error num=156 + status=failed; runtime mid-batch → completedWithErrors + error 208; **PARSEONLY parse errors DO surface** (`SELECT FROM` under `SET PARSEONLY ON` → error 156, failed) — my earlier "PARSEONLY loses errors" was a false alarm from testing `SELEC 1`, which is an OBJECT-resolution error that PARSEONLY legitimately skips (SSMS behaves identically). Orchestrator→host→webview all forward messages (verified by read). Most likely Karl saw these on a build predating the observability fix (journal blind) or the tab bug hid the Messages tab. Re-verify with diagnostics now flowing.

**#2 (estimated plan) — confirmed working** (probe: SHOWPLAN_XML column + plan row delivered; journal plan.parse [ok] plans=1).

**#1 (Windows integrated auth) — hard tedious limitation, honest by design.** Pure-JS tedious has no Windows SSPI/Kerberos, so true integrated auth (logged-in identity, no creds) is impossible; ts-native declares `auth.integrated: unsupported "driver.noIntegratedAuth"` and canOpen fails BEFORE creds. Correct answer is FALL BACK to sts2-local (C# SqlClient does integrated auth). QS has the capability-fallback prompt; OE v2 connect path (oeV2SessionRegistry.connect) does NOT yet — it calls openSession directly. **Follow-up (not yet done):** add canOpen→resolveCapabilityFallback to oeV2SessionRegistry. Future support path: msnodesqlv8 behind ITdsDriver port (native dep, deferred).

**Debug Console "SQL Data Plane" page — `b476153b5` (advances residual 1 / task #10).** New page: default/normalized backend + availability KPIs, registered-backends table (state/realm/session count/stale-config/safe last-error), ts-native aggregate counters, and each backend's own snapshot (ts-native driver + active overrides + live sessions/in-flight query state) as a JSON tree. Wiring: `DcGetSqlDataPlaneStatusRequest` → controller reshapes passive `statusSummary()` + `tsNativeObservabilityCounters()` via a pure, vscode-free `debugConsoleStatus.ts` projection (unit-tested: raw last-error MESSAGE dropped — only code/retryable/serverErrorNumber ride; snapshot carries no SQL/rows). Nav entry + page in the DC webview. Bundles: 0 tedious in activation, RPC present in both.

Full unit suite after all three commits: green except the 4 known pre-existing non-mine failures (sqlScripting strict-host, sqlLanguage sys catalog, OE v2 stableProfileId, CopilotChatEntry flake). New tests: queryStudioViewState (resolver contract + contributed fallback), tsNative/observability (observer→diag + privacy), sqlDataPlaneDebugConsoleStatus (projection + privacy redaction).

**REMAINING (task #10 residual):** support-capsule EXPORT command (ring buffer via FeatureCaptureStore + traceCodec) and observability-contracts registry entries for the sqlDataPlane.tsNative.* diag family. Plus #1 OE v2 capability-fallback wiring (above). Residuals 2–5 from entry 10 unchanged.

## 12 — 2026-07-14 — Centralized fallback + full Debug Console + upstream merge (autonomous)

Karl asked to (a) map Windows-auth into the fallback path *everywhere* — support sts v1 / sts v2 / native-ts side-by-side in one process, even multiple backends per document — optimizing UX around ts-native's faster startup + no-.NET-runtime reach; (b) fill out the Debug Console with deep internal state for perf analysis + repro-grade bug reports; (c) integrate remote changes + resolve conflicts in vscode-mssql, sqltoolsservice, perftest.

**Centralized capability fallback — `96f235843`.** `SqlDataPlaneService.openSessionWithFallback(params, opts, interaction)` is now the ONE path every consumer shares: resolves backend (explicit per-document override > remembered fallback for this profile > configured default), runs `canOpen`, and on a capability miss applies policy (prompt | auto | off) via `resolveCapabilityFallback`. A policy-chosen route is remembered per `profileFingerprint` (no re-prompt on reconnect); an explicit override is never remembered; a config change clears the map. Routed consumers: QS `documentSessionBinding` (primary connect + aux/master sessions now mirror the user session's backend via `serviceForProfile`), OE v2 (`oeV2SessionRegistry` takes an injected opener = openSessionWithFallback — closes entry-11 residual #1/OE-v2), and metadata (`metadataStore`/`metadataStoreService` resolve via `serviceForProfile` so background hydration silently follows a fallen-back profile). Production `vscodeFallbackInteraction` = MODAL warning for prompt (a connection decision must be deliberate) + info toast for auto; stays out of vscode-free `providerSuggestions`. 7 tests (sqlDataPlaneFallbackOpen): prompt/auto/off, remember-no-reprompt, override-wins-not-remembered, config-clears, SQL-auth-no-prompt. vectorCatalogProbes + OE v2 mocks updated for the serviceForProfile/opener seam.

**Debug Console filled out — `6e89c6b78`.** Extended the SQL Data Plane page with: host environment (node/platform/arch, extension version, behavior-affecting `mssql.sqlDataPlane.*` settings incl. 4 timeouts + tsNative overrides — values only), a per-backend capability matrix (support/fidelity/limit+unit/reasonCode/source — exactly what the fallback policy reads), and this-session fallback routes (non-reversible fingerprint → backendKind) + a Fallback policy KPI. Projector took an options-object (environment/fallbackPolicy/rememberedFallbacks/capabilities); controller gathers from process.versions + config + registry `rememberedFallbacks()`/`capabilitySnapshot()`. Privacy holds (last-error message still dropped; fingerprints non-reversible); projection test extended.

**Upstream merge — `2ccd2e770`.** sqltoolsservice fast-forwarded (0-behind), perftest merged (0-behind), vscode-mssql merged origin/dev/query (9 commits: QS results-grid perf overhaul — bounded streaming grid windows, offscreen reclaim, wide-viewport column projection, positioned message windows, faster spill, sweepResultStack). ONE conflict: both branches fixed Results→Messages stranding — KEPT mine (Results always sticks; terminal handler sole authority) and DROPPED upstream `isQueryStudioRunStartPending`/`pendingRunStart` as redundant (mine also closes the fast-terminal + back-to-back windows their run-start guard misses). All other upstream app.tsx changes preserved. Post-merge: extension + webviews typecheck clean; **5010 passing**; packaging guard holds (0 tedious in dist/extension.js, 284 in dist/tsNativeProvider.js). All 3 repos now 0-behind origin/dev/query (vscode-mssql 17 ahead — NOT pushed; awaiting Karl).

**NEW pre-existing UPSTREAM failure surfaced (NOT mine — rowStore.ts + queryStudioResultsCore.test.ts byte-identical to origin, my branch never touched them):** "RowStore > vectorAnalysis scans never re-admit spilled pages; grid reads do (VEC-3)" — `expected 2000 to be above 2000`. Upstream "bound streaming grid windows" made a grid read over spilled pages keep memoryBytes at the cap instead of growing (test expects grid reads to re-admit for "viewport warmth"). Could be a stale test OR a real viewport-warmth regression upstream. LEFT for upstream to reconcile; flagged to Karl.

**REMAINING (unchanged + new):** task #10 residual = support-capsule EXPORT command + observability-contracts registry for sqlDataPlane.tsNative.* (Debug Console data+page + OE v2 fallback now DONE). Residuals 2–5 from entry 10. NEW #6: investigate/report the upstream RowStore VEC-3 failure. Push of dev/query (all 3 repos) awaits Karl's go.
