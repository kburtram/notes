# Schema Visualizer — Progress Journal

Journal wins over EXECUTION_PLAN.md on conflict. Newest entries at the bottom.
Format: `## <n> — <date> — <task> — <status>` then facts: what changed, evidence, decisions, next.

## 1 — 2026-07-14 — SV-R0 setup — IN PROGRESS

- Karl ratified the design + dropped the normative review addendum (`schema_visualizer_design_review_addendum.md`, REVIEWED WITH REQUIRED REVISIONS — addendum wins). Answers to design §9 recorded as D5–D8/D14/D19–D20 in EXECUTION_PLAN.
- **Code moved to `C:\repos\test\langsrv2`** (another codegen owns langsrv). Heads: vscode-mssql `cfa51b8fa`, sqltoolsservice `018140e2`, perftest `5abc11f` — all dev/query, clean, NEWER than addendum-reviewed heads → symbol re-verification required before every edit.
- Docs remain in `C:\repos\test\langsrv\notes\vscode-ext-refactor\schema-visualizer\`.
- perftest: npm install + build green in langsrv2. vscode-mssql monorepo install running.
- Session tasks #1–#8 map SV-R0..R10.

## 2 — 2026-07-14 — SV-R0 COMPLETE

- EXECUTION_PLAN.md written (decision log D1–D20 = Karl's answers + addendum §22). PROGRESS.md started.
- Marker registration (contracts-first, addendum §13): perftest `793c3cc` "core: register schemaVisualizer marker family + legacy schemaDesigner.ready" — open/refresh/rebase/commit.handoff/publish begin-end pairs (extensionHost, sameProcessMonotonic), driftDetected instant, modelReady/layout.begin/end/ready webviewMarks (epochAligned, calibrated-plane note), derived metrics `mssql.schemaVisualizer.open|open.toReady|layout|commit.handoff|publish` + `mssql.schemaDesigner.init.toReady`. Attrs = counts + safeEnums only.
- Re-vendored into vscode-mssql `8e8d575ae` "core: re-vendor observability contract…". Contracts suite 27/27 green incl. vendorSync (byte-identical) + parity.
- vscode-mssql monorepo npm install green; perftest install+build green (langsrv2).
- NOTE for later phases: langsrv2 heads include the upstream merge (alt-endpoints entry 12) — known upstream RowStore VEC-3 failure exists on this branch per memory; don't attribute it to us.

## 3 — 2026-07-14 — SV-R1 + SV-R2 COMPLETE

**SV-R1 (`qs:` commit `5f9058713`, vscode-mssql):** substrate identity + exact detail per addendum §5.
- H3 extended (23 cols): system/user type ids, type schema, TYPE_NAME base, UDT/assembly flags, raw max_length BYTES, precision/scale, collation, default constraint name+definition (LEFT JOIN sys.default_constraints), identity seed/increment as exact TEXT via CONVERT(nvarchar(64), sql_variant) (LEFT JOIN sys.identity_columns), computed definition+persisted (LEFT JOIN sys.computed_columns). column_id now RETAINED (works even for old-shape rows). H5 + `*_referential_action_desc` mapped by `fkActionFromDesc` explicit switch (numbers/null → UNKNOWN — the 0/1 swap hazard). H5B + constraint_column_id + parent/referenced column ids.
- catalogModel: 24 new SoA arrays (19 column + 2 FK action + 3 pair), `ColumnDetailInfo`/`FkReferentialAction`/`FkActionState` types, read surface sets new fields ONLY when known (old-fixture deep-equals unaffected except 2 updated expectations where fixtures DID carry ids).
- Codec: cm1→**cm2**, append-only canonical fields, strict validation (parallel lengths, sentinel sym ranges ≥ -1, FK action enum), direct adoption; cm1 disk payloads = clean modelVersion miss (manifest test now mutates to "cm1" as the mismatch case).
- Tests: new `metadataCatalogDetail.test.ts` (action mapping, exact seed text > MAX_SAFE_INTEGER, alias UDT, nvarchar(max) raw -1, old-shape tolerance, cm2 round-trip byte-stability, unchanged-rehydration hash invariance). largeCatalogFixture emits new real-query shapes.
- **Gates:** full suite **4927/12/5 — all 5 = documented pre-existing** (sqlScripting strict-host, sqlLanguage sys catalog, RowStore VEC-3 upstream, OE v2 stableProfileId, CopilotChatEntry flake). **Live probe:** exact H3/H5/H5B SQL (extracted from compiled module) green via sqlcmd vs local SQL 2025, master + msdb (msdb exercised sysname alias rows, 180 output lines). Probe script: scratchpad/probe-h3-live.js + probe-h3.sql.
- LESSON: piping background runs through `tail` truncates the captured log AND masks exit codes — capture full logs to a file, read the file.

**SV-R2 (`sv:` commit, vscode-mssql):** `src/schemaVisualizer/model/` — schemaVisualizerModel.ts (Available<T> known/unknown + known(null)=definitively-absent, stable-id builders, capability matrix types incl. `columnIdentityGrade`), catalogToVisualizerModel.ts (pure, tables-only P0, §5.8 capability mapping, vector dims derived from documented 8+4n layout), visualizerFingerprint.ts (`svf_` sha256; excludes generation/timestamps/descriptions; `complete` flag = informationalScript capability), visualizerToGraphProjection.ts (O(1) nodeById, danglingEdges surfaced for raced-DDL/subset, action label "Unknown"). 7/7 tests green incl. THE invariant (gen N vs N+1 ⇒ same fingerprint ⇒ same graph ids).
- DEVIATION: edit-op/reducer types deferred to SV-R6 (where semantics + tests land together) — R2 exit gate (stable-ID + unchanged-generation invariants) did not require them; empty scaffolding rots.

**WORKSPACE FINDING:** langsrv2's dev/query is a DIFFERENT lineage than langsrv (no tsq2/ts-native commits, no tedious dep; langsrv head b476153b5 is NOT an ancestor). Metadata/QS/OE/sts2 substrate all present — irrelevant for this build, but don't assume langsrv facts (e.g. tedious availability) here. Live probes: use sqlcmd (ODBC 17, -C flag) not tedious.
- Next: SV-R3 graph extraction (core:) — verify A-12 impurity against current components first.

## 4 — 2026-07-14 — SV-R3 COMPLETE (`core:` commit, vscode-mssql)

- A-12 VERIFIED at current heads: SchemaDiagramFlow imports state provider/selector/eventBus/diff utils/change context/CopilotReviewToolbar; table node likewise + undo dialogs. Only flowLayout/flowDimensions are pure — and flowLayout has the A-13 per-edge `nodes.find`.
- Built `src/webviews/common/schemaGraph/` (NOTE: branch convention is webviews/common, NOT webviews/shared — FluentResultGrid precedent): schemaGraphTypes (plain data + collapse split + aria helpers), schemaGraphDimensions (constants pinned to legacy by parity TEST), schemaGraphLayout (pure Dagre kernel, O(1) Map resolution, hidden filtering; cancellation/size policy explicitly caller-owned), SchemaGraphTableNode (presentational, legacy visual parity, `left-/right-<columnId>` handles + consolidated hidden handles), SchemaGraphCanvas (props-driven shell; read-only contract: draggable, never connectable/deletable; onInitialized seam for the ready marker).
- DECISION (D8 refined): legacy page BYTE-UNTOUCHED — neutral components are new code sharing the visual language, not a rewiring of the shipping page; legacy convergence onto the shared canvas = SV-R11 follow-up candidate. §20.17 respected: nothing stateful copied (state/context/diff/undo machinery deliberately NOT carried).
- §10.4 guard = unit test scanning schemaGraph + schemaVisualizer sources for forbidden legacy import specifiers (works independent of eslint config).
- 6/6 tests green (layout determinism/visibility, 1000-node/2000-edge correctness, dimension parity, collapse, aria, import guard). Extension + webview typechecks clean.
- Next: SV-R4 read-only surface (controller/manager/activation/page/flag/command/OE v2 entry/properties/large-catalog policy).

## 5 — 2026-07-14 — SV-R4 COMPLETE (`sv:` commit, vscode-mssql) — **P0 (SV-R0..R4) DONE**

- **Architecture:** vscode-free `schemaVisualizerSession.ts` core (leases-only, one-snapshot pins, oeBrowse freshness, fingerprint drift §6.1/§6.4, search-first >500 tables + objectId subsets + fkNeighborhood) under a thin `schemaVisualizerController.ts` (WebviewPanelController; markers open/refresh/driftDetected, counts-only attrs) + `schemaVisualizerActivation.ts` (palette cmd `mssql.schemaVisualizer.open`, OE v2 db-node menu via `oe2:kind=database` + `config.mssql.schemaVisualizer.enabled` when-clause, PERF_MODE probe `mssql.perf.schemaVisualizerOpen` for the R10 scenario, panel map keyed §8.2 non-secret). Page: `webviews/pages/SchemaVisualizer/` (app + properties pane) over the shared schemaGraph shell; marks layout.begin/end + modelReady + ready (perfMarkAfterNextPaintComputed, §11.5 semantics); drift/stale/limited banners; positions preserved across refresh by stable ids.
- **Wiring:** bundle entry `schemaVisualizer` (dist/views/schemaVisualizer.js, 39 KB — heavy libs in shared ESM chunks); setting `mssql.schemaVisualizer.enabled` (default false, requires sqlDataPlane); mainController activation beside OE v2 (same profiles/tokens deps).
- **KEY BOUNDARY LESSON:** sharedInterfaces must be SELF-CONTAINED — importing session types into sharedInterfaces/schemaVisualizer.ts dragged the node-typed metadata stack into the WEBVIEWS tsconfig program (Buffer/crypto errors). Serializable result shapes now DECLARED in the shared interface; the session implements them (`export type X = SchemaVisualizer.X`). Webview may import only pure model modules (schemaVisualizerModel, visualizerToGraphProjection — NEVER visualizerFingerprint, it imports crypto).
- **Gates:** no-v1 tripwire green (OE v2 spy pattern verbatim: SqlToolsServiceClient.sendRequest + ConnectionManager.connect notCalled across all read flows); honesty (columns failed ⇒ diagramNodes limited + fingerprintComplete false, tables list still served); drift protocol (unchanged content across generations ⇒ fingerprintChanged=false; DDL ⇒ true — mutable FakeScript fixture via Object.defineProperty getter on `events`); bundle-budget suite green; contracts conformance green. **Full suite 4945/12/5 — same 5 pre-existing** (pass count +18 = our tests).
- **DEVIATION (sequencing):** the "live open lane + first-paint" R4 gate item lands with the SV-R10 perftest scenario (real VS Code + provisioned SQL + waitForMarker `mssql.schemaVisualizer.ready`) — unit lane can't spawn sts2-local, and this lineage has NO ts-native/tedious (workspace finding, entry 3).
- Residual for SV-R9: `featureFor()` in perf/perfTelemetry buckets `mssql.schemaVisualizer.*` (and schemaDesigner) into "system" — add mappings in a tiny `core:` commit with the conformance test updated.
- Lint gotchas: repo has no react-hooks eslint plugin (don't reference its rules in disable comments); floating promises on sendNotification need `void`.
- Next: SV-R5 informational SQL generator; then SV-R10 scenario (+schema-designer.ready legacy mark = SV-R9 slice); then SV-R6..R8.

## 6 — 2026-07-14 — SV-R5 COMPLETE + SV-R9 slice + SV-R10 scenarios (run in flight)

**SV-R5 (`sv:` commit):** `src/schemaVisualizer/scripting/schemaVisualizerSqlGenerator.ts` — pure, canonical-model-driven, INFORMATIONAL_HEADER label, correctness-first divergences from legacy documented in-code (] escaping, named PK/UNIQUE in key-ordinal order, named defaults, exact identity text, UDT two-part names, actions only when KNOWN — NO_ACTION emits nothing, UNKNOWN omits + warns). Unknown facts: omit + warn (computed w/o definition ⇒ column omitted). Rendered in properties pane with warnings. 5/5 tests incl. byte-exact golden.

**SV-R9 slice (`core:` commit `236fce77f`):** legacy `mssql.schemaDesigner.ready` webview mark (perfMarkAfterNextPaintComputed after initialization commit — behavior-free) + featureFor buckets schemaVisualizer/schemaDesigner + conformance pins.

**SV-R10 (perftest, uncommitted until run validates):** `schema-designer-open` extended (wait ready + `mssql.schemaDesigner.init.toReady` metric, official:false boundary); NEW `schema-visualizer-open` (userSettings sqlDataPlane+schemaVisualizer, PERF_MODE probe cmd + waitForMarker ready; metrics `mssql.schemaVisualizer.open` host-phase + `.open.toReady` rendered, both unofficial §14.5); `DESIGNER_PHASE_MAP` auto-selected in head-to-head for the designer pair (host/model + rendered semantics); example config `config.schema-visualizer.local.jsonc` (STS2_SQLSERVER_SQLLOGIN_CONNSTRING, both scenarios one run). perftest contracts 27/27; CLI vitest 166 pass / 1 fail = centralStore.integration (needs PerfCentral container localhost,14333 — NOT ours, central-observability live lane).
- **A/B DEBUGGING TRAIL (log-analysis loop):**
  1. Run 1: config schema-invalid (missing `regression`/`output` blocks — the designers template tail I hadn't read). Fixed.
  2. Run 2: legacy 4/4 PASS (new `mssql.schemaDesigner.ready` mark works live!); visualizer reps invalid in ~0.5s — result.json errors: "no saved profile visible for the visualizer probe" → scenario needed `provisionConnectionProfile` step (OE v2 browse pattern). Fixed.
  3. Run 3: visualizer opens (markers flow), `sv/getModel` fails in 8ms `metadataUnavailable`. Enriched the error with freshness facts + added a 30s startup-grace retry loop in the controller (real-user race: data plane starting right after window load).
  4. Run 4: retries exhausted — `source=none,freshness=unavailable,validation=fullRefresh` persisting. DIFFERENTIAL: `objectexplorerv2-browse` PASSES (but only proves the SERVER catalog; database H-queries untested by it). Seeded `mssql.sessionDiag.enabled` into the scenario → journal in captured profile shows **`rpc.v2/initialize.end` status=error**: the sts2 handshake itself fails → every hydrate dies in ~0.9ms.
  5. ROOT CAUSE: config env was empty — the extension used the PACKAGED STS (no sts2). config.oe.local.jsonc sets `MSSQL_SQLTOOLSSERVICE` to a local Debug build. Fixed config → langsrv2 STS build dir; `dotnet build` of langsrv2 ServiceLayer in flight (was never built in this duplicate).
  - LESSON (journal-worthy): any scenario exercising sts2/data-plane MUST carry the `MSSQL_SQLTOOLSSERVICE` env to a local build — the packaged service fails `v2/initialize` and everything downstream reports as metadataUnavailable. sessionDiag-in-captured-profile is the fastest live-failure microscope (grep segment-*.jsonl for status=error).
  6. Run 5: activation failed — the override dir needs `SqlToolsResourceProviderService.exe` too: built `Microsoft.SqlTools.ResourceProvider` + copied its bin output into the ServiceLayer bin (no-clobber). langsrv2 STS ServiceLayer built for the first time here (dotnet build, both csproj).
  7. Run 6 (single rep): **PASS, wallclock 1299ms.** Full A/B 8/8 PASS.

## 7 — 2026-07-14 — SV-R10 COMPLETE — **FIRST DESIGNER A/B: −77.5% wallclock**

- Commits: perftest `perf: schema-visualizer-open scenario + designer head-to-head (SV-R10)`; vscode-mssql `cde714ad1` "sv: startup grace + diagnosable unavailable errors".
- **A/B RESULTS** (run `2026-07-14T15-50-31Z_07d9ae37`, 3 official reps/side, SAME run/session/machine, local SQL 2025, PerfHarness seed-v1):
  - `scenario.wallclock`: 4432.8ms → **998.3ms (−77.5%)**
  - host/model ready (`schemaDesigner.init` → `schemaVisualizer.open`): 2026.4ms → **488.6ms (−75.9%)** — DacFx full-model load vs metadata acquire+adaptation
  - open → rendered (`init.toReady` → `open.toReady`): 2370.0ms → **573.0ms (−75.8%)** — both = first meaningful paint (§14.2 fair endpoints)
  - Report: `perftest/head-to-head-schema-designer-vs-schema-visualizer.html`; runs in perf-runs/.
- This also closes the SV-R4 deferred gate items: live open lane green (visualizer opened, hydrated over live sts2 data plane, rendered, ready mark fired) + first-paint marker proven end-to-end through the calibrated plane.
- The `mssql.schemaVisualizer.open` phase median 488.6ms includes the startup-grace path (first getModel after activation); wallclock medians ~1.0s include probe dispatch + webview boot. Phase metrics remain UNOFFICIAL until baseline history (§14.5); wallclock official (backend-A/B twin precedent).
- REMAINING in R9: support-capsule-style Dc section (optional P2), privacy canary sweep test for visualizer diag (counts-only attrs — enforced by code, worth a pinned test), central-obs vocabulary registration if ever uploaded.
- Next: SV-R6..R8 (edit ops + reducer, v1 updater fixes, classic resolver + handoff state machine + preview token + live publish lane).

## 8 — 2026-07-14 — SV-R6 COMPLETE (`sv:` commit); SV-R7 DELEGATED (agent in flight)

- **SV-R6:** `model/schemaVisualizerEdit.ts` (identity-first V1 op union: table add/drop/rename/schema, column add/drop/rename/type/nullability, FK add/drop/actions; TableRef/ColumnRef/ForeignKeyRef existing-by-id | new-by-localId; EditTypeSpec discrete facts; typed EditConflict codes; entity-key helpers) + `model/schemaVisualizerEditReducer.ts` (EditableModel = baseline projection with baseline names preserved; pure applyEdit with clone-per-op; normalizeOperations §7.4 — add+drop cancel, last-wins coalesce, drop-cascade incl. columns-of-dropped-table, FK-after-structure partition; rebaseOperations §7.5 — sequential, stop-at-first-conflict with appliedCount, log never discarded). 9/9 tests (`schemaVisualizerEditReducer.test.ts`). Full suite at R10 gate: 4950/12/5 (same 5 known).
- tsgo NOTE: `if (!x.ok)` did NOT narrow the ApplyResult union under tsgo — use `if (x.ok === false)` (matches repo style elsewhere).
- **SV-R7 delegated to background agent** (sqltoolsservice, `core:` prefix): DeepCompareColumn default/computed omissions + UpdateColumnProperties computed-path fix + §9.3 tests. Result pending — verify its commit + test counts when it reports.
- **SV-R8 REMAINING** (the last major arc): `handoff/` — classic publish resolver (§8.1 — dedicated seam over ConnectionManager, resolve-at-command-time, never cache secrets), handoff state machine (§8.3), preview token (§8.4), correlation onto v1 baseline (§6.6 — ids first, names for diagnostics; copy untouched entities VERBATIM from v1 baseline), replay EditableModel→`SchemaDesigner.Schema` with v1 GUIDs (new entities = fresh UUIDs §4.5), exact request sequences + disposal rules (§8.5/§8.6, try/finally + leak test), §17.4 fake-v1 suites, §6.7 preview→publish drift check, minimal edit-mode UI wiring + drift-while-dirty rebase UX, live publish + conflict lanes (STS2_SQLSERVER_SQLLOGIN_CONNSTRING; create table→preview→publish→verify catalog→cleanup).
- Current commit stack (langsrv2): vscode-mssql `8e8d575ae` vendor, `5f9058713` qs R1, R2 sv, R3 core schemaGraph, R4 sv surface, R5 sv generator, `236fce77f` core marks, `cde714ad1` sv startup-grace, R6 sv reducer; perftest `793c3cc` core contracts, R10 perf scenario; sqltoolsservice: R7 agent pending.

## 9 — 2026-07-14 — SV-R7 COMPLETE (agent) + SV-R8 CORE COMPLETE

**SV-R7 (sqltoolsservice `7e5f99d46`, `core:`, agent-built):** both §9 defects CONFIRMED live at head and fixed — DeepCompareColumn now compares DefaultValue/IsComputed/ComputedFormula/ComputedPersisted (null-safe ordinal, matching sibling comparisons); UpdateColumnProperties gained a both-computed branch (formula/persisted/persisted-nullability, DacFx-capability-guarded, then returns so regular-column updates skip computed columns); decision logic extracted to internal pure helpers. 24/24 filter-scoped tests; 10 of them PROVEN to pin the defects (fail on reverted utils). CAVEATS: DacFx TableColumnViewModel application path needs the integration lane (DacSchemaDesigner requires live conn); identity-wire exactness is the client D12 decision, untouched.

**SV-R8 core (`sv:` commit, vscode-mssql):** `handoff/replayEditsToLegacySchema.ts` (Correlator: exact-then-CI-fallback per §6.6, hard typed ambiguity/absence conflicts, verbatim untouched-entity copies via deep-copied baseline, minted UUIDs via injectable newId, `toOnAction` explicit switch — 0/1 swap pinned) + `handoff/schemaVisualizerHandoff.ts` (§8.3 state machine; §6.5 pre-preview: refreshLive→refuse-incomplete→fingerprint→normalize+rebase→ONLY THEN resolver+createSession (creds disposed in finally)→correlate/replay→getReport→token §8.4; §6.7 pre-publish fingerprint re-check; §8.5 disposal on every exit + created/disposed ledger; §15 publish-ok/refresh-failed separation). 15/15 tests: exact §8.6 sequences ×4 paths, same-session, edit/drift invalidation, resolver-fails-no-session, second-preview-replaces, publish-race retryable, ledgers balanced, replay verbatim/UUID/rename/CI-CS suites.
- tsgo narrowing REMINDER: use `x.ok === false` blocks (negation + early-return does NOT narrow under tsgo).

## 10 — 2026-07-14 — SV-R8b PLUMBING COMPLETE — session end state

- vscode-mssql `d786b5e4f` "sv: publish handoff wiring…" — carries BOTH the R8 core (handoff machine + replay + 15 tests; the intended separate core commit silently failed on hooks in a background run — lesson: verify `git log` after background commits) and the R8b wiring:
  - `handoff/classicPublishResolver.ts`: `createClassicPublishResolver` (ONLY ConnectionManager touchpoint, command-time re-resolution, dispose-scoped credentials, `confirmClassicHandoff` prompt behind NEW setting `mssql.schemaVisualizer.confirmLegacyHandoff` default true) + `legacyPortOverService` adapter (createSession mints request sessionId via randomUUID; response sessionId wins).
  - Controller: lazy machine over `session.refreshLiveFull()` (NEW session API — FULL model + fingerprint, never the search-first subset; updates lastServedFingerprint content-consistently), drift → `handoff.notifyDrift()`, RPCs `sv/previewChanges|publish|cancelPreview` + `sv/edited` notification, markers commit.handoff/publish begin-end (outcome enums + counts + dataLoss boolean only), dispose → machine.dispose.
  - sharedInterfaces: serializable PreviewToken/params/results mirrors (handoff module itself is node-typed, webview-unsafe).
  - mainController supplies `schemaDesignerService` + `_connectionMgr` seams.
- Gates: both typechecks clean; 35/35 visualizer suites (handoff 15 + session 5 + reducer 9 + graph 6, incl. §10.4 import guard covering the new handoff files); prior full suite 4974/12/5 (same 5 pre-existing).
- tsgo REMINDER again: ternary `x.ok ? a : x.code` does not narrow either — use if-blocks.

**REMAINING — SV-R8c (next stretch START HERE):**
1. **Edit-capture UI** (webview): edit mode toggle (gate on capabilities.columnIdentityGrade + informationalScript), op creation from properties pane/graph (start minimal: rename table/column, drop table/column, add table dialog, nullability toggle, FK actions dropdown), local op log + undo/redo cursor (rebaseOperations over ops.slice(0,cursor) — engine ready), `sv/edited` on every log change, drift-while-dirty banner offering Rebase (client-side rebase vs latest model; conflicts listed per op) / Discard.
2. **Preview/Publish UX**: Preview Changes button → sv/previewChanges(ops) → render DacReport facts (possibleDataLoss/requireTableRecreation/warnings + report entries) + Publish/Cancel → sv/publish(token) → success toast (+ separate refresh-failed warning per §15) → clear op log + reload model.
3. **Live lanes** (§17.9, STS2_SQLSERVER_SQLLOGIN_CONNSTRING): publish lane (add table + rename column + composite FK → preview → publish → force refresh → verify catalog → cleanup) and conflict lane (edit → external DDL via sqlcmd → preview blocked/rebase). Could ride a PERF_MODE probe command sequence or a manual dogfood checklist first.
4. Full-suite + EXECUTION_PLAN status flips + acceptance checklist (§21) sweep; consider promoting `mssql.schemaVisualizer.open` metric maturity after baseline history.
5. SV-R11 deferred decisions unchanged (DAB, views, Table Explorer entry, legacy-page convergence onto shared schemaGraph).

## 11 — 2026-07-14 — UPSTREAM MERGE (all three repos), post-merge gates green

- Pulled origin/dev/query into all repos (notes already current). Upstream vscode-mssql carried the ENTIRE tsq2/ts-native stack (the langsrv workstream: dp foundation → tedious engine → transcoders → Debug Console SQL Data Plane pages → capability-fallback centralization) + QS results-grid perf overhaul; sqltoolsservice carried sts2 perf work; perftest carried the QS backend A/B + allocation tracing.
- **Conflicts:** exactly ONE — perftest `headToHead.ts`: upstream replaced static phase maps with metric-presence inference (`inferPhaseMappings` + `AUTO_PHASE_FAMILIES`) + a diagnostics table. Resolution: kept upstream's architecture as default; `phaseMapForScenarios` now returns the DESIGNER_PHASE_MAP only for the known designer pair and `undefined` otherwise → falls through to inference (`options.phases ?? phaseMapForScenarios(...) ?? inferPhaseMappings(...)`).
- vscode-mssql auto-merged cleanly (package.json + vendored contract text-merged by git). Contract outputs REGENERATED from the merged registry in perftest; the vscode-side text-merged vendored file turned out semantically identical (vendorSync NORMALIZES the prettier formatting the vscode hook applies — whitespace + key-quoting; no re-vendor commit needed).
- **Gates:** sqltoolsservice builds + R7 SchemaDesigner 24/24; perftest contracts 27/27 incl. cross-repo vendorSync; vscode-mssql npm install (tedious dep arrived), both typechecks clean, full build green, all 58 visualizer/substrate/conformance tests green, **full suite 5065/12/5 — same 5 documented pre-existing failures** (~90 new upstream tests absorbed, zero breakage of our stack, zero breakage BY our stack).
- End state: vscode-mssql ahead 11, sqltoolsservice ahead 2, perftest ahead 3 (nothing pushed — pull+resolve only, per Karl).
- LESSON (now twice-bitten, journaled hard): a background `git commit` chained with `&&` can fail on hooks INVISIBLY, and lint-staged REVERTS working-tree changes on failure — always verify `git log` + `git status --porcelain` after any backgrounded commit. Also: "lint-staged prevented an empty git commit" means prettier normalized your staged change back to HEAD — check whether the change was actually formatting-only before fighting it.

**Old SV-R8b list (superseded by entry 10):**
1. `SchemaVisualizerClassicPublishResolver` vscode impl: mirror `schemaDesignerWebviewManager.ts` (connectionManager.getConnectionString + access-token path); inject via activation; policy: reuse OE v2's confirmLegacyHandoff-style confirm prompt (D3/§8.1 list).
2. LegacySchemaDesignerPort adapter over `src/services/schemaDesignerService.ts` (createSession takes connectionString/accessToken/database — check exact ISchemaDesignerService signature; sessionId rides responses).
3. Edit-mode UI: op capture from properties pane/graph (or minimal "edit ops" panel first), undo/redo cursor buttons, drift-while-dirty rebase banner (machine.notifyDrift wired to session.onDidChange fingerprintChanged), Preview Changes → DacReport display (reuse legacy report UI? it lives in SchemaDesigner page — simplest: render report facts in our pane) → Publish (token) → post-publish refresh.
4. `sv/previewChanges`, `sv/publish`, `sv/cancelPreview`, `sv/notifyEdit` RPCs + controller wiring of the machine (markers commit.handoff.begin/end + publish.begin/end registered SV-R0).
5. Live publish + conflict lanes (§17.9) against STS2_SQLSERVER_SQLLOGIN_CONNSTRING; leak assertion via ledger.
6. Full-suite + PROGRESS close-out; EXECUTION_PLAN status flips.

## 12 — 2026-07-15 — SV-R8c COMPLETE (edit UI + apply dialog) + legacy UX parity (Karl's list)

- **Working repo note:** built in `C:\repos\test\langsrv\vscode-mssql` this session (converged lineage; langsrv2 fast-forwards from it). One `sv:` commit; see git log.
- **Parity (Karl 2026-07-15):** panel title/icon = legacy exactly; React Flow chrome themed via NEW provider-neutral `webviews/common/schemaGraph/schemaGraphFlowColors.css` (dark minimap/controls/background — §10.4 forbids importing the legacy page CSS, so it is a copy, kept in sync by eye); bezier width-1 edges, FK edge labels removed; toolbar rebuilt on Fluent Toolbar in legacy order (Apply Changes | Undo Redo | Add Table Delete | Auto Arrange Filter | Refresh + search/badges right); Filter = table-checkbox popover over the existing subset/requestModel path.
- **R8c:** op log + cursor as edit authority; `projectEditableGraph` (pure) renders edits live with baseline ids preserved + baseline-only facts joined by identity; `schemaVisualizerTableDraft` (pure) draft↔ops diff — identity-based renames; table editor OverlayDrawer (add/edit, type picker with discrete facts); FK actions/drop in sidebar; Apply Changes dialog over sv/previewChanges→report→publish(token) with cancelPreview on close; drift-while-dirty banner (Rebase via normalize+rebaseOperations, conflict-stop honest, or Discard); sv/edited on every log change; edit gate = columnIdentityGrade available.
- **Handoff confirmation (Karl's #5):** NOTHING new needed — R8/R8b already wired mainController→activation→resolver (`createClassicPublishResolver` over ConnectionManager, command-time creds) + `legacyPortOverService`; the v1/DacFx session is created when the user clicks Apply Changes (preview), exactly the intended design (D3). No design MD required.
- Tests: +10 pure-seam (schemaVisualizerEditUi.test.ts); graph 58 + SV band 433 green; full suite result recorded at commit time.
- **REMAINING (SV-R8c residuals → next stretch):** add-FK UI (op + reducer + replay ready; drawer tab pending); §17.9 live publish + conflict lanes against STS2_SQLSERVER_SQLLOGIN_CONNSTRING; manual dogfood checklist; Export/Show-Definition toolbar ports if Karl wants full button parity; EXECUTION_PLAN status flips + §21 acceptance sweep after live lanes.
